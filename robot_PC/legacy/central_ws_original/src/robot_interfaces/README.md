# robot_interfaces

Empty ROS 2 interface package skeleton for future messages, services, and actions.


The Pinky handoff uses standard `std_msgs/msg/String` topics and does not add a custom interface to this package.
